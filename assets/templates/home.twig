{% extends 'master.twig' %}

{% block title %}{% if posts|length == 1 %}{{ posts[0].title }}{% else %}Home{% endif %}{% endblock %}

{% block content %}
  {% for post in posts %}
    {% include 'post.twig' with {'post': post} %}
  {% endfor %}
{% endblock %}
